# Iconvex Specs ICU Archive B

Transparent runtime-table shard for historical ICU archive codecs 351–700 in
`iconvex_specs`. Install `iconvex_specs`; it starts this provider automatically.

The generated mapping data retains the Unicode License. Provider code is
LGPL-2.1-or-later, matching Iconvex and GNU libiconv libraries.
